// 8. Set up an initState
const initState= {
    posts: []
}
// 7 create a relatively empty reducer
const rootReducer = (state = initState, action) => {
    return state
}

// 9. export the rootReducer and go back to index.js
export default rootReducer